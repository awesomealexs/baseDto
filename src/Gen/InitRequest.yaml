$schema: http://json-schema.org/draft-07/schema
type: object
namespace: BaseDto\Gen
properties:
  account:
    serialize: user-account
    type: string
  nonce:
    type: string
    description: 'one-time (pseudo) random value in hex format, must be unique for each request'
  signature:
    type: string
  test1:
    type: object
    serialize: asd
    properties:
      value1:
        type: string
      value9:
        serialize: asd123
        type: int
